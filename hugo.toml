baseURL = 'https://zac.events/'
languageCode = 'en-us'
title = "Zac's Summer Movie Night"
theme = 'papermod2'

[markup]
    [markup.goldmark]
        [markup.goldmark.renderer]
            unsafe = false
[params]
    hideFooter = true
    [params.label]
        icon = '/moviebanner8.png'
#        text = "Zac's Summer Movie Night3"
    [params.assets]
        disableFingerprinting = true
        styleSheetVersion = 3